# Changelog

## 2026-08-12 — release/qa 历史会话恢复与多视图回退

- 回退上游 v0.2.3 的 Dashboard / List / Gantt 三个项目视图，不再在项目页暴露“仪表盘 / 列表 / 甘特图”切换。
- 删除 `DashboardView.tsx`、`IssueListView.tsx`、`GanttView.tsx`，清理对应 `dashboard-*`、`issue-list-*`、`gantt-*` 样式和三视图专用 presentation 聚合代码。
- 移除 `dhtmlx-gantt` 依赖，`package.json` 与 `package-lock.json` 同步更新。
- 保留普通看板卡片的 Codex 进度条能力；该能力仍由现有 `BoardColumn` 使用，不依赖被回退的三视图。
- 调整 Codex/Claude/OMP 会话运行时识别与跳转逻辑，历史会话会按线程归属推断运行时，避免非 Codex 会话误走 Codex 原生打开路径。
- Codex 注入脚本在打开已有线程前会展开原生侧栏任务区，提升已关联历史会话跳转成功率。
- 议题详情补充当前会话与历史相关会话的说明，降低用户误判当前会话和历史会话含义的概率。
- 验证：`npm run typecheck`、`node --test test/board-interactions.test.mjs test/inject.test.mjs`、`npm run build`、`npm test` 均通过；`npm run build` 已刷新当前 Codex 注入页。

## 2026-08-11 — 设计方案与文档目录整理

- 新增 `docs/knowledge/designs/taskboard-overall-design.md`，把当前任务面板的产品定位、真实操作路径、多运行时会话、项目知识闭环和目录边界整理为整体设计方案。
- 根目录不再放置过程方案和历史 proof 截图；知识库方案归入 `docs/knowledge/designs/`，历史方案归入 `docs/knowledge/designs/legacy/`，历史截图归入 `docs/screenshots/proofs/`。
- `docs/knowledge/index.md` 增加整体设计入口和目录使用约定，明确正式知识、历史方案、公开截图和内部 proof 截图的存放边界。

## 2026-08-11 — 独立本地分发整理

- 删除 Cloudflare Worker、D1、R2、Wrangler、云端登录、云端代理和一次性迁移脚本；仓库分发形态回到本地 Node.js 服务、SQLite 和 SSE。
- `README.md` 按独立安装重新组织，补充 Codex、Claude Code、Oh My Pi 会话创建、点击跳转、当前/历史会话关联和评论新建会话能力。
- macOS 启动器不再硬编码个人目录或固定 Homebrew Node 路径；安装时记录 Node/Codex 可执行文件路径，并改用中性 bundle id。
- 项目目录映射写入 SQLite 项目记录，`/api/device-workspaces` 可直接读取该映射，不再依赖本机额外配置文件。
- `taskctl` 删除 `cloud login/status/logout`，Skill 文档同步移除云端协作命令。

## 2026-08-10 — 项目持续知识中心

- 每个已映射本地目录的项目新增“项目知识”视图，正式知识保存在项目仓库 `docs/knowledge/`，已落地技术方案统一放入 `docs/knowledge/designs/`；`changelog.md` 继续作为真实项目变更时间线。
- 项目初始化分析、整项议题、单条或多条评论、进入审核、完成验收、项目问答、知识过期修订和阶段复盘都只生成待确认提案；提案经过逐文件、逐行差异审核后才发布到项目文件。
- 正式知识支持主题浏览、全文搜索、带来源项目问答和确定性新鲜度检查；文件来源按当前 Git blob 校验，议题与评论来源按乐观版本校验。
- SQLite 保存提案及来源快照，项目文件读取、代码分析和安全发布只在本地 Taskboard 服务中执行；目录、分支和 worktree 均受项目映射边界约束。
- 发布使用基线摘要检查、临时文件替换和失败回滚；重复提交已写入的相同内容可幂等完成，真实并发修改会明确阻止覆盖。
- e-taskboard 已使用同一规范初始化自身项目知识；本地服务、知识服务和看板触发入口均有自动化回归覆盖。

## 2026-08-07 — 新建任务会话按任务项目归组

- 修复从 A 项目任务发起新会话却沿用上一激活项目、固定归入 `taskboard` 的问题；故障会话的 Codex `session_meta.cwd` 实际记录为 taskboard 目录，确认不是侧栏显示问题。
- 新建会话的目录与项目信息改为根据任务自身的 `projectId` 获取，不再从当前打开的 Taskboard 项目或 Codex 宿主工作目录回退。
- 创建前先切换工作区并选择对应的 Codex 原生项目；只有侧栏确认目标项目已激活后才进入新会话并发送指令，切换失败或随后离开目标项目时直接停止并提示。
- 议题详情和已发表评论下的“新建会话处理”共用同一创建链路，因此同时修复。
- 验证：注入脚本语法检查、定向回归测试、TypeScript 类型检查、Web 生产构建和 `git diff --check` 通过；完整注入测试另有 2 条与本次无关的既存失败（自动化解析契约、已有会话打开旧断言）。

## 2026-08-07 — 中文编号列表完整展示

- 议题描述或评论中的数字编号即使写成 `2.内容`、编号后没有空格，也会按独立有序列表项展示，不再被 Markdown 合并进上一条。
- 保留原始存储内容，仅在展示时兼容常见中文输入习惯。

## 2026-08-07 — 议题支持迁移项目

- 议题详情属性区新增“项目”，选择目标项目并确认后可直接迁移；迁移完成后自动进入目标项目中的新编号详情。
- 本地 SQLite 使用原议题 ID 完成迁移，描述、评论、附件、状态、优先级、标签、负责人、工作流、会话关联、开发上下文、截止日期和重复设置保持不变。
- 目标项目按自身编号序列分配新标识；现有跨项目关系会在确认框中明确列出，并在迁移事务中原子解除，接口同时返回解除清单。
- `taskctl` 新增 `issue transfer ID --project PROJECT_ID`，支持乐观版本和 Codex 会话归因。

## 2026-08-07 — Codex 重启后任务面板 CSP 启动闭环

- 根因确认：Codex 新 Renderer 首次恢复时，任务面板 iframe 会先被宿主页面 CSP 拒绝，CDP 明确返回 `net::ERR_BLOCKED_BY_CSP`；此前的原地附着流程虽然随后启用了 CSP bypass，但没有产生新的文档导航，因此绕过设置无法补救已经失败的 iframe。
- 修复注入器启动判定：发现注入脚本尚未进入当前 Renderer，或任务面板 iframe 已落入 `chrome-error://chromewebdata/` 且 `unreachableUrl` 指向本地任务面板时，只执行一次受控 bootstrap reload，使 CSP bypass 在新文档导航前生效。
- 普通心跳恢复、监督器重新附着和已有健康 iframe 仍保持原地附着，不刷新 Codex 主页面；bootstrap reload 仅用于新 Renderer 或已被 CSP 阻断的确定性失败现场。
- 新增结构化日志 `cdp-csp-bootstrap-reload`，记录目标 Renderer 与触发原因，便于区分首次启动修复和普通重连。
- 真实现场验证：原故障现场每约 17 秒出现一次注入器退出/监督器重启；修复后日志命中一次 `taskboard-frame-unreachable` bootstrap，随后父页面隐藏启动占位、显示 iframe，iframe 完整渲染看板数据，监督器、注入器和本地服务持续存活且未再进入重启循环。
- 验证：Node 语法检查、注入器定向测试 23/23、`git diff --check` 均通过。

## 2026-08-07 — Chromium 本地网络访问拦截修复

- 修复 Codex 新版本把 `app://` 页面加载 `127.0.0.1` 任务面板判定为公共页面访问本地网络、并显示浏览器拦截页的问题。
- Dock 启动器和注入器启动 Codex 时增加精确的 `--disable-features=LocalNetworkAccessChecks`；仅关闭当前专用 Codex 进程的本地网络权限检查，不使用范围更大的 `--disable-web-security`，本地服务继续只监听 `127.0.0.1`。
- 注入成功判定不再只看 iframe URL；必须等任务面板发送 ready、使 iframe 真正可见后才报告 `frameLoaded=true`，浏览器错误页不会再被误报为加载成功。
- iframe 在看板内打开项目或议题后允许追加 `view/project/issue` 等路由参数；注入器按同源、同路径及启动参数子集校验，不再因正常 URL 状态变化误判加载失败并重启。
- 当前窗口通过重新导航恢复了任务面板；启动参数将在下次通过 **Codex Taskboard** Dock 入口启动 Codex 时生效。
- 验证：启动器与注入器语法检查、注入器定向测试 25/25、`git diff --check` 均通过。

## 2026-08-06 — Codex 注入器重连不再刷新主页面

- 修复驻留注入器把 Codex Renderer 短暂繁忙误判为断线的问题：宿主心跳超时由 1.5 秒调整为 5 秒，连续 3 次失败才重连；WebSocket 确认关闭时仍立即恢复。
- 心跳延迟、恢复和确需重连均写入带时间、目标和失败次数的结构化日志，后续可区分 Renderer 繁忙与真实连接中断。
- supervisor、Dock 手动恢复和 watch 重连统一使用 `--attach-existing` 原地附着现有 Renderer，不再进入 `Page.reload` 分支。
- `npm run build` 只刷新当前任务看板 iframe；需要更新注入脚本时继续使用显式 `npm run codex:refresh`，避免普通构建重启驻留注入器。
- 真实运行验证：supervisor 切换到新版注入器时，Codex 主进程 PID 与 React 根渲染次数保持不变；随后执行 Web 生产构建，Codex 主进程、驻留注入器 PID 与 React 根渲染次数均保持不变。任务面板状态为 `pageMounted=true`、`pageVisible=true`、`frameLoaded=true`。
- 验证：注入器行为测试 21/21、Node 与 shell 语法检查、Web 生产构建、`git diff --check` 均通过。

## 2026-08-06 — 任务面板启动握手、评论解除关联与会话标题

- 修复任务面板服务和 iframe 均已加载，但 Codex 仍永久停在“正在启动任务面板…”的问题：面板现在会对每次宿主上下文探测重复发送就绪确认，不再依赖首次挂载时的一次性消息。
- 已在真实 Codex 运行时重新附着并验证 `pageMounted=true`、`pageVisible=true`、`frameLoaded=true`，构建刷新后再次验证通过。
- 已发表的评论在“更换关联会话”菜单中新增“解除关联”；解除只清空该评论的会话 UUID，不删除评论、附件或 Codex 会话。
- 已关联会话标签在鼠标悬停或键盘聚焦时会显示右上角叉号，可直接解除关联；菜单内的解除入口继续保留。
- 本地 SQLite 支持 `threadId: null` 的显式解除语义；省略 `threadId` 仍保持原关联。无其他任务或评论引用时同步清理议题的冗余历史会话记录。
- `manage-taskboard` 工作流在议题确认进入 `in_progress` 后，会尽力把当前 Codex 会话改名为 `<议题编号> · <议题标题>`；改名失败不阻塞任务执行。
- “新建会话处理”“在当前会话处理”和“在关联会话处理”会在 Skill 与指令校验后真正点击 Codex 语义发送按钮；单纯“关联会话”仍只建立关系。
- 已有会话发送成功后直接使用已知 `threadId` 完成确认，不再扫描全部 Codex session 反查会话，避免已成功执行却继续占用“上一条会话确认中”的全局锁。
- 未完成的会话请求与回执最多独占 5 分钟，超时自动释放，避免一次未发送请求长期阻塞后续新建。
- 验证：本次定向测试 5/5、TypeScript 类型检查、Web 生产构建通过；本地覆盖“关联后解除”的数据回归。

## 2026-08-06 — 新建会话预识别关联修复

- 修复“Codex 会话已经创建，但议题或评论没有自动关联”的前置竞态：不再根据侧栏新增行及其顺序猜测会话 UUID。
- 每次新建请求会在首条指令中携带唯一关联标记；常驻注入器只在本地 Codex 会话记录中精确匹配该标记并返回对应 UUID，并发创建多个会话也不会互相串联。
- “尚未识别出 UUID”的请求改为持久化事务；注入脚本刷新、任务面板关闭重开或常驻注入器短暂重启后会继续解析，不再静默丢失。
- 会话文件解析只扫描请求产生后的日期目录和最近文件，避免轮询全部历史会话影响其他任务。
- 删除“检测到多个侧栏行就终止关联”的旧逻辑；如果目标会话文件尚未生成则保持待处理并重试，不写入猜测结果。
- 现场议题 `3417FD193D6E-10` 已确认关联到实际创建的会话 `019fd55d-010a-76d1-90ec-dcde7169b1c3`。
- 已核对议题 `3417FD193D6E-9` 的实际会话关联正确，并清除其遗留的前端待关联记录，避免继续阻塞后续新建请求。
- 新增会话解析器与宿主桥接测试，覆盖唯一匹配、暂未生成、重复标记拒绝选择及宿主回执。

## 2026-08-06 — 评论会话关联可恢复事务修复

- 修复“新会话已创建、议题已出现新 UUID，但原评论仍未关联”的漏回写：此前评论关联依赖 iframe 内存中的一次性 `requestId` 和单次 `postMessage`，页面刷新或回执时序变化会静默丢失结果。
- 前端将待关联的 `requestId`、议题、项目和评论 ID 持久化；iframe 重载后会恢复未完成请求，不再依赖旧 React 实例内存。
- 注入器将确认后的最终 UUID 作为待确认回执持久化，并每秒重复投递；只有前端确认评论和议题均已落库并返回 ACK 后才清除。
- 评论回写每次重新读取最新版本；遇到版本冲突会重新获取并重试。已关联到同一 UUID 时直接视为成功，避免重复更新。
- 议题回写同样重新读取最新版本并处理并发冲突；回执、请求、议题和评论 ID 必须匹配，过期记录 24 小时后清理。
- 已将评论 `e7b82922-2553-4ba9-af0b-0209f19225d4` 补偿关联到实际新会话 `019fd537-e62a-7db3-9d73-30966f40738a`；作者、正文和 1 个附件均保留，评论版本从 2 更新为 3。
- 真实恢复验证：写入一笔幂等关联后强制重载任务面板 iframe，宿主回执与前端 pending 均自动清空；数据库中的评论 UUID 正确且版本保持 3，证明恢复、重投和 ACK 闭环没有产生重复写。
- 验证：`node --check`、TypeScript 类型检查、Web 生产构建、会话/注入器定向测试 15/15、评论主路径定向测试 1/1、`git diff --check` 均通过；真实 Codex 运行时加载了当前源码哈希。

## 2026-08-06 — 评论新会话串联修复

- 新建会话不再读取 Codex 侧栏的旧 `active` 行，也不再用正文是否出现议题编号推断会话。
- 创建前同时快照已有最终 UUID 和侧栏行；只追踪本次新增的侧栏行，并等待其从 `client-new-thread:*` 转换为连续稳定的最终 UUID 后再回写。
- 每次新建携带独立 `requestId`，任务、评论和宿主回执必须完全匹配；过期回执不会修改数据。
- 同时出现多个新增会话时停止自动关联并提示手动选择，无法唯一确认时不写入评论或议题。
- 注入器监听会话行 ID 的原地变化，临时 ID 转最终 UUID 时能继续完成确认。
- `codex:refresh` 现在先重启常驻注入器以加载最新注入源码，再刷新 iframe；修复此前只刷新页面、代码已改但运行时仍使用旧脚本的问题。
- 已将评论 `d922e932-fd9c-4938-b0d0-2889ae3c6744` 从错误的 `019fd2ad...` 修正到红框第一条真实会话 `019fd505-eeaa-7a42-a3d3-554859b97b36`。
- 扫描当前项目 20 条带会话评论，并与各会话初始议题编号交叉核对；修正后未发现其他明确串联。
- 验证：会话 ID、常驻注入器和评论主路径定向测试通过；TypeScript 类型检查与 Web 生产构建通过；真实运行时源码哈希与本地最新注入源码一致。

## 2026-08-06 — 白屏与评论会话关联时序修复

- 修复 Codex 没有选中原生会话时，任务面板找不到新的原生 frame、左侧入口已选中但注入页仍保持隐藏的问题；已有注入容器现在会在原挂载面恢复显示。
- 评论列表 API 在成功响应缺少 `comments` 或评论附件数组时统一归一化为空数组，避免异步回填 `undefined` 后触发 `comments.length` 崩溃和 React 白屏。
- 注入器启动本地服务时强制使用 `127.0.0.1` 和固定端口，消除构建刷新与常驻监督器同时拉起回环/全网卡两个服务实例的竞态。
- 评论编辑器恢复为单一“发表评论”动作，发表时不创建、不关联、也不跳转会话。
- 每条已发表评论新增“新建会话处理”和“关联已有会话/更换关联会话”；操作成功后才写入评论会话关联并打开对应 Codex 会话。
- 新建会话链路携带 `commentId`，最终 UUID 产生后同时回写议题和原评论，避免关联到错误评论。
- 议题持久化的 `threadIds` 会与 Codex 当前可见会话目录合并；原生侧栏暂时读不到会话时，评论仍可选择该议题已有会话。
- 验证：`npm run typecheck`、`npm run build`、`git diff --check` 通过；白屏、评论时序、会话 ID、注入器和服务定向测试 73/73 通过；真实 Codex 在缺少原生 frame 的失败现场恢复显示，详情页 7 条评论均呈现独立会话动作。

## 2026-08-06 — Codex Taskboard 持久启动与会话可靠性修复

### 启动与恢复

- 新增 `Codex Taskboard` macOS Dock 启动器，在 Codex 启动前启用仅监听 `127.0.0.1:9229` 的 CDP。
- 新增被动 `LaunchAgent` 监督程序：只观察 Codex/CDP 状态并注入任务面板，不再请求退出、重启或强制关闭 Codex。
- Dock 入口在 Codex 已运行时兼作手动“恢复任务面板”入口。
- 监督程序使用完整 CDP 实例签名识别 Codex 重启，即使端口不变也会重新注入。
- 强制同一 CDP 端口只保留一个常驻注入器，避免重复宿主事件和重复输入。
- `npm run build` 只刷新已打开的 iframe，不再创建第二个常驻注入器。
- 保留官方 Codex 应用本体，不修改、不替换、不重新签名应用文件。

### 会话关联可靠性

- 统一 Codex 会话 ID 为小写裸 UUID；兼容读取 `local:` 和 `urn:uuid:` 前缀。
- 拒绝 `client-new-thread:*` 等临时会话 ID，防止无法恢复的 ID 写入任务和评论。
- 新会话建立关联时只接受当前激活会话产生的最终 UUID，不再回退并继承上一个会话，修复议题串会话。
- 打开关联会话时等待 Codex 原生内容恢复，验证目标会话确实激活；首次点击未生效时走原生路由兜底，修复白板状态。
- 新增 `scripts/repair-codex-thread-ids.mjs`，支持只读扫描和显式 `--apply` 修复旧数据。
- 本地 SQLite 增加 `task_threads` 历史关联；`threadId` 保持当前会话，`threadIds` 返回去重、有序的会话历史。
- 评论会话进入议题历史，但不会覆盖议题当前会话。

### 看板功能

- 议题详情展示“当前会话”和可展开的历史相关会话，并支持在已有会话中继续跟进。
- 子议题区域支持直接打开完整议题编辑器创建子议题，保存后自动建立父子关系。
- 项目首页支持收藏优先、拖拽排序、本地别名、归档与分类/收藏排序视图。
- 默认新建议题状态统一为 `todo`。
- 标签选择器、任务筛选、会话筛选和详情页交互同步适配当前数据模型。

### 已修复的用户可见问题

- Codex 关闭重开后任务面板入口消失。
- 旧监督程序重复弹出“退出 ChatGPT?”对话框。
- 两个注入器同时输入导致新会话出现 `$$`，随后 Skill 选择超时。
- 临时会话 ID 造成“未找到对话”和 `invalid session id`。
- 新议题错误继承旧议题会话。
- 关闭任务面板后原生会话首次点击丢失，主区域显示白板。

### 数据与安全边界

- 任务数据仍保存在 `.data/taskboard.sqlite`，数据库、备份、日志和截图不进入 Git。
- 安装器会备份修改前的 Dock 配置。
- 本次数据修复前已创建 SQLite 一致性备份，修复后 `PRAGMA integrity_check` 为 `ok`。
- 已交叉审计现有任务、历史和评论的 9 组会话关联，修复后错误关联为 0。

### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。
- 启动、注入、会话 ID、本地多会话、子议题和看板交互定向测试：87/87 通过。
- 三个 shell 脚本通过 `zsh -n`。
- LaunchAgent 和 App Bundle plist 均通过 `plutil -lint`。
- `git diff --check` 通过。
- 已通过真实 Codex 关闭/重开、任务面板重新注入和用户界面复验。
- 尚未执行真实 macOS 整机重启验收；电脑重启后的最终验证仍待完成。
- 全仓 `npm test` 仍存在 AI Chat、自动化、样式和工作流等既有宽泛断言失败，因此发布为 Draft PR；上述本次核心路径定向测试均已通过。
