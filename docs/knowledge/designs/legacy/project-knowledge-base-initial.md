# 项目知识库技术方案（初版）

> 本文保留最开始调研产出的完整设计，仅用于与当前基础版对比，不随后续收缩方案同步修改。

## 1. 结论

e-taskboard 可以为每个已映射本地目录的项目提供独立知识库。

第一版不建议上向量数据库，也不建议把知识正文存入 Taskboard SQLite。建议采用“项目源码与议题记录 → 项目内 Markdown Wiki → Taskboard 阅读与更新入口”的结构：

- 知识库是项目仓库的一部分，使用 Git 审计、分支和协作；
- Taskboard 通过现有 `workspacePath` 定位它，不新增项目字段；
- Codex 负责初始化、增量更新、查询后回写和健康检查；
- Taskboard 负责项目入口、页面阅读、新鲜度状态和用户发起操作；
- 小规模用 `index.md` + 文件搜索，实际规模和查询数据证明不够时再引入 qmd。

## 2. 调研结论

Karpathy 的 LLM Wiki 不是传统 RAG 的另一个包装，它的关键是把已经归纳、交叉引用和冲突检查过的知识持久化，不在每次问答时从原始文档重新推导。

原始方案包含三层：

1. Raw sources：不被知识维护者改写的事实来源。
2. Wiki：持续更新的 Markdown 页面、概念、实体、比较和综合结论。
3. Schema：约束目录、页面类型、引用、写入和维护流程的规则。

核心操作是：

- Ingest：读取新来源，更新相关页面、索引和日志；
- Query：先查索引和相关页面，回答后将有长期价值的结论回写；
- Lint：检查过期、冲突、孤立页面、断链和知识缺口。

原始方案明确指出，约 100 个来源、数百页的规模下，`index.md` 仍然可以有效导航，不需要预先引入 embedding 架构。

参考资料：

- [Karpathy: LLM Wiki 原始构想](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)
- [LLM Code Knowledge Base：针对可变代码库的改造](https://gist.github.com/decon3/4604aedabac6de04be003694c5ceec5d)
- [yopedia：可运行的持续演进实现](https://github.com/yologdev/yopedia)
- [qmd：本地 Markdown 混合检索](https://github.com/tobi/qmd)

## 3. 现有系统证据与可复用链路

### 3.1 项目和本地目录已建立稳定关系

- `web/src/App.tsx` 在新增和选择项目时维护项目与本地目录的关系；
- `web/src/api.ts` 通过 `/api/projects` 和 `/api/projects/:id/development-contexts` 获取项目和工作区；
- `server/app.mjs` 已能从项目映射、Codex 项目和会话上下文解析真实工作区，并将其解析为 Git 根目录；
- `server/database.mjs` 中的 `projects.workspace_path` 已足以表达本地项目根目录。

因此，知识库根目录可固定从项目工作区派生，不需要新增 `knowledge_path` 或类似字段。

### 3.2 项目视图已有扩展位置

`web/src/App.tsx` 的项目顶部已有“议题看板 / 节点模式”视图切换区。知识库应作为同级的“知识库”视图，不应塞进单个议题详情。

### 3.3 本地执行能力已存在

`server/ai-chat.mjs` 和 `server/ai-chat-catalog.mjs` 已能在项目工作区内启动 Codex 会话并发现 Skill。第一版可复用该能力执行知识库初始化和更新，不需要再建一套模型调度层。

## 4. 目标操作链路

```mermaid
flowchart LR
    A["项目页选择知识库"] --> B["Taskboard 解析设备上的项目根目录"]
    B --> C["读取 docs/wiki/index.md 和页面元数据"]
    C --> D["展示目录、正文和新鲜度"]
    D --> E["用户发起初始化、更新、问答或检查"]
    E --> F["Codex 在当前项目工作区内读取受控来源"]
    F --> G["写入 Markdown Wiki，更新 index.md 和 log.md"]
    G --> H["Taskboard 刷新并展示可审查的结果"]
```

可验收的主路径为：

> 项目页的“知识库”入口 → 用户点击“初始化” → Codex 在当前项目生成 `docs/wiki/` → Taskboard 展示索引、页面和新鲜度 → 用户可查看 Git diff 审查知识变更。

## 5. 知识库结构

### 5.1 目录

```text
docs/wiki/
├── SCHEMA.md
├── index.md
├── log.md
├── map.md
├── glossary.md
├── overview/
├── architecture/
├── decisions/
├── patterns/
├── flows/
├── gotchas/
└── guides/
```

- `SCHEMA.md`：知识库目录、页面类型、引用规则、忽略范围和维护流程；
- `index.md`：按知识类型分组的内容目录，每页一行摘要和新鲜度；
- `log.md`：只追加的初始化、更新、问答回写和检查日志；
- `map.md`：代码目录到知识页的映射，用于快速定位“某项能力在哪里”；
- 内容页按知识类型组织，不镜像代码目录。

### 5.2 页面最小元数据

```yaml
---
title: 议题状态流转
type: flow
updated_at: 2026-08-06
sources:
  - kind: file
    ref: server/app.mjs
    revision: git:3f5b8d1
  - kind: issue
    ref: 3417FD193D6E-5
    revision: 17
---
```

仅保留四个必要字段：

- `title`：展示和检索名称；
- `type`：定义页面所属目录和必要的格式约束；
- `updated_at`：人类可读的最后更新时间；
- `sources`：每个事实来源的类型、引用和版本。

不提前增加信心分、负责人、审批状态等字段；它们只在后续出现明确业务需求时引入。

## 6. 事实来源和新鲜度

### 6.1 来源优先级

1. 项目规则、README、技术方案、ADR 等已有文档；
2. 真实代码、配置、测试和 Git 历史；
3. Taskboard 议题描述、评论和已验收结论；
4. 外部资料，必须保留可访问的原始链接。

代码库不是不变的 raw source。Git commit 和 blob hash 提供不可变快照，因此每个知识页记录其依赖文件的版本。

Taskboard 议题和评论可以修改或删除。当用户执行“沉淀到知识库”时，应先将当时的议题正文、评论、附件引用和版本号组成不可改写的摄取快照，再从快照更新 Wiki。快照不直接展示为知识结论。

### 6.2 新鲜度检查

新鲜度判定为纯脚本逻辑，不调用模型：

- Git 项目：比较元数据记录的 blob hash 和当前文件 blob hash；
- 非 Git 目录：比较文件 SHA-256；
- Taskboard 议题：比较议题或评论的 version；
- 任一来源版本变化时，页面标记为“可能过期”，不自动改写结论；
- Codex 更新时读取差异，判断是否影响页面事实。不影响时只刷新来源版本。

## 7. Taskboard 产品设计

### 7.1 知识库视图

在项目顶部视图切换中新增“知识库”，页面分为三区：

- 左侧：按 `index.md` 分类的页面目录和搜索；
- 中间：Markdown 正文和页内链接；
- 顶部状态区：未初始化、已就绪、部分可能过期、映射不可用四种状态，以及“初始化 / 更新 / 检查”操作。

用户打开页面时只执行文件扫描和新鲜度检查，不自动发起 Codex 会话或写入仓库。

### 7.2 议题沉淀

在议题详情中增加独立的“沉淀到知识库”操作。第一版由用户主动触发，不与 `done` 状态自动绑定，避免无价值或尚未稳定的议题结论污染 Wiki。

触发后：

1. Taskboard 将当前议题、评论、附件元数据和版本生成摄取快照；
2. Codex 对照代码和现有 Wiki 判断哪些结论具有长期价值；
3. 创建或更新最少量页面，同步 `index.md` 和 `log.md`；
4. 用户通过 Git diff 审查实际变更。

### 7.3 查询和回写

“询问知识库”使用项目现有 Codex 会话能力：

1. 先读 `index.md`，再打开相关页面；
2. 涉及当前实现的结论必须回查代码或配置；
3. 回答保留知识页和源文件引用；
4. 只有用户点击“保存到知识库”后，才将新综合结论回写。

## 8. 最小接口面

知识库文件读写只发生在本地 companion，不由云端 Worker 直接访问设备文件系统。

| 接口 | 用途 | 是否写入 |
|---|---|---|
| `GET /api/projects/:id/knowledge` | 获取初始化状态、索引、页面摘要和新鲜度 | 否 |
| `GET /api/projects/:id/knowledge/pages/:path` | 读取单个 Markdown 页面 | 否 |
| `POST /api/projects/:id/knowledge/snapshots/issues/:issueId` | 生成本次摄取的议题快照 | 是 |
| `POST /api/projects/:id/knowledge/check` | 执行确定性新鲜度与断链检查 | 否 |

初始化、更新、问答和 Lint 的内容生成不封装为新的服务端模型接口，而是通过项目会话调用内置的知识库 Skill。

所有页面路径必须解析到项目根目录的 `docs/wiki/` 内，拒绝绝对路径、`..` 逃逸和越过根目录的符号链接。这是本地文件接口的必要输入边界。

## 9. 本地与云端边界

- 本地模式：Taskboard 直接使用项目的 `workspacePath`；
- 云端协作模式：任务数据来自云端，知识库文件由每台设备的本地 companion 使用本机项目映射读取；
- 设备间的 Wiki 协作依靠 Git 提交和同步，不复制到 Taskboard 业务数据库；
- 没有有效本地映射时，知识库视图仅显示“请先映射项目目录”，不尝试回退到服务器上的其他目录。

## 10. 分期计划

### 阶段 A：可阅读的项目 Wiki

- 增加“知识库”项目视图；
- 实现知识库状态、索引和页面读取接口；
- 增加内置项目知识库 Skill；
- 支持从未初始化状态创建骨架和首批核心页面；
- 支持 Markdown 正文、相对链接和源引用展示。

该阶段完成后，“每个项目可以有独立知识库”的主路径成立。

### 阶段 B：可增量维护

- 页面来源版本及确定性新鲜度检查；
- 基于 Git diff 更新受影响页面；
- 议题“沉淀到知识库”和摄取快照；
- 断链、孤立页和重复标题检查。

### 阶段 C：可查询和持续复利

- 项目内问答和源引用；
- 用户确认后将有价值的问答回写到 Wiki；
- 基于页面数、索引大小和实际查询耗时决定是否引入 qmd；
- 只在数据证明 `index.md` + 文件搜索不足时增加混合检索。

## 11. 验收标准

### 主路径

- 已映射本地目录的项目可以打开“知识库”视图；
- 未初始化时可由用户发起初始化，在当前项目生成 `docs/wiki/`；
- 初始化后 Taskboard 能显示索引和页面正文；
- 每个知识页有可回跳的源文件或议题引用；
- 用户可以通过 Git diff 查看本次知识库写入。

### 增量和边界

- 修改被知识页引用的文件后，页面被确定性标记为“可能过期”；
- 更新操作只修改受影响页面、`index.md` 和 `log.md`；
- 未映射项目不读取或写入其他工作区；
- 云端模式下的知识文件操作只由本地 companion 执行；
- 第一版不引入项目知识库专用数据表、向量数据库或后台自动写入。

## 12. 不在第一版范围

- 将全部源码按函数或类生成 API 参考文档；
- 通过定时任务在无人确认时自动改写 Wiki；
- 为每个项目创建独立的向量服务或外部索引集群；
- 把 Wiki 作为代码和正式业务文档的替代品；
- 在没有源码、配置、测试、议题或用户说明支持时生成确定性服务、接口、字段或调用链结论。
